# Tells Modulith to treat '.service' and '.event' sub-packages as open public APIs globally
# Scan everything, but completely ignore the global events package
org.springframework.modulith.additional-packages=!com.example.events